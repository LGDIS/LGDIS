ワークフロー(フィールドに対する権限)初期設定
1.ワークフロー(フィールドに対する権限)の初期データのcsvを作成する
※  rake db:seedコマンドを実行する前に、下記のコマンドを実行する
$ cd LGDIS/plugins/lgdis/db/seeds
$ ruby create_workflow.rb
$ cd /home/rails/LGDIS

※ ワークフロー(フィールドに対する権限)の初期登録データを変更する場合は、このCSVファイルを変更する

2.初期データを挿入する
$ rake db:seed RAILS_ENV=production

